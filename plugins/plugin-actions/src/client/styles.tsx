import { CSSProperties } from 'react';

export const styles = {
	windowHeader: {
		position: 'relative',
		padding: '15px 15px 0',
	} as CSSProperties,
	windowBody: {
		position: 'relative',
		padding: 15,
	} as CSSProperties,
	ul: {
		listStyle: 'none',
		padding: 0,
		margin: 0,
	} as CSSProperties,
	li: {} as CSSProperties,
};
