import { CSSProperties } from 'react';

export const styles = {
	windowBody: {
		height: 250,
	} as CSSProperties,
	keyboard: {
		position: 'absolute',
		right: 0,
		top: 11,
	} as CSSProperties,
	contextMenuItem: {
		minWidth: 200,
		paddingLeft: 0,
		paddingRight: 40,
	} as CSSProperties,
};
